import { describe, it, expect } from 'vitest';

// Stub — will be populated in Step 3 after parseReleaseDate is exported
describe('parseReleaseDate', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
